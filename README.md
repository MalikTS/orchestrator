# Orchestrator — Lightweight Cluster Manager (No Containers)

Система оркестрации микросервисов **без контейнеризации** на Go.  
Реализует три компонента:  
- **Workload** — сервис полезной нагрузки  
- **Agent** — управляет запуском workload на хосте  
- **Controller** — центральный оркестратор (service discovery, масштабирование, балансировка)

Соответствует требованиям ТЗ:  
✅ Без БД  
✅ Без Docker/K8s  
✅ Ручное масштабирование через API  
✅ Состояние кластера в `/status`

---

## 🚀 Быстрый запуск (локально)

### 1. Сборка
```cmd
go build -o workload.exe .\cmd\workload
go build -o agent.exe .\cmd\agent
go build -o controller.exe .\cmd\controller
```

## 2. Запуск (в отдельных терминалах)
### Терминал 1 — контроллер:
```cmd
.\controller.exe
```

### Терминал 2 — агент:
```cmd
.\agent.exe -port=9001 -node-ip=127.0.0.1 -workload-bin=.\workload.exe
```
### Можно запустить несколько агентов на разных портах (-port=9002, -port=9003...) для эмуляции кластера.


3. Управление
Задать количество реплик:
cmd
curl -X POST http://localhost:8080/deploy -H "Content-Type: application/json" -d "{\"replicas\":3}"
Проверить состояние кластера:

cmd
curl http://localhost:8080/status
Пример ответа:

json
{
  "desired_replicas": 3,
  "actual_replicas": 3,
  "replicas": [
    {
      "id": "replica-0",
      "node_ip": "127.0.0.1",
      "port": 8000,
      "uptime": "2m10s",
      "started_at": 1768327146

📁 Структура проекта
orchestrator/
├── cmd/
│   ├── workload/      # Сервис полезной нагрузки
│   ├── agent/         # Агент на ноде
│   └── controller/    # Центральный контроллер
└── internal/types/    # Общие типы данных

💡 Примечания
Все процессы запускаются как обычные исполняемые файлы (без изоляции).
Агент автоматически регистрируется в контроллере и отправляет ping каждые 10 секунд.
Порты для workload выделяются автоматически: 8000, 8001, 8002...
Для Windows используй curl.exe; в Git Bash или WSL — просто curl.
